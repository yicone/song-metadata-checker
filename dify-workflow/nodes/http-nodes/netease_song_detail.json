{
  "node_type": "http-request",
  "title": "网易云音乐 - 获取歌曲详情",
  "description": "从网易云音乐 API 获取歌曲的详细信息",
  "config": {
    "method": "GET",
    "url": "{{#env.NETEASE_API_HOST#}}/song/detail",
    "headers": {
      "Content-Type": "application/json"
    },
    "params": {
      "ids": "{{song_id}}"
    },
    "timeout": 10000,
    "retry": {
      "enabled": true,
      "max_retries": 3,
      "retry_interval": 1000
    }
  },
  "outputs": [
    {
      "variable": "netease_song_details",
      "type": "string"
    }
  ],
  "error_handling": {
    "fail_branch": true,
    "on_error": "stop"
  }
}
